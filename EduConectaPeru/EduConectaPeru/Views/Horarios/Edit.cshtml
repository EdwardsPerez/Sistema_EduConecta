@model EduConectaPeru.Models.Horario

@{
    Layout = null;
}

<style>
    .modal-header-custom {
        background: linear-gradient(135deg, #42A5F5 0%, #2196F3 100%);
        color: white;
        border-radius: 15px 15px 0 0;
        padding: 20px 25px;
    }

    .modal-body-custom {
        padding: 30px;
    }

    .form-section {
        margin-bottom: 30px;
        padding-bottom: 20px;
        border-bottom: 2px solid #f1f3f5;
    }

        .form-section:last-child {
            border-bottom: none;
        }

    .form-section-title {
        font-size: 1.1rem;
        font-weight: 600;
        color: #495057;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 10px;
    }

        .form-section-title i {
            color: #2196F3;
            font-size: 1.3rem;
        }

    .form-label-custom {
        font-weight: 600;
        color: #495057;
        margin-bottom: 8px;
        font-size: 0.9rem;
    }

    .form-control-custom {
        border: 2px solid #e9ecef;
        border-radius: 10px;
        padding: 12px 15px;
        transition: all 0.3s ease;
    }

        .form-control-custom:focus {
            border-color: #2196F3;
            box-shadow: 0 0 0 0.2rem rgba(33, 150, 243, 0.15);
        }

    .btn-cancel {
        background: #6c757d;
        color: white;
        border: none;
        padding: 12px 30px;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
    }

        .btn-cancel:hover {
            background: #5a6268;
            transform: translateY(-2px);
            color: white;
        }

    .btn-update {
        background: linear-gradient(135deg, #42A5F5 0%, #2196F3 100%);
        color: white;
        border: none;
        padding: 12px 30px;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(33, 150, 243, 0.4);
    }

        .btn-update:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(33, 150, 243, 0.6);
            color: white;
        }

    .text-danger {
        font-size: 0.875rem;
        margin-top: 5px;
    }

    .info-box {
        background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
        border-left: 4px solid #2196f3;
        padding: 15px;
        border-radius: 8px;
        margin-bottom: 20px;
    }
</style>

<div class="modal-header modal-header-custom">
    <h5 class="modal-title">
        <i class="bi bi-pencil-square"></i> Editar Horario
    </h5>
    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
</div>

<form id="editForm" asp-action="Edit" method="post">
    <div class="modal-body modal-body-custom">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <input type="hidden" asp-for="HorarioId" />

        <div class="info-box">
            <i class="bi bi-info-circle"></i>
            <strong>Editando:</strong> @Model.Curso - @Model.DiaSemana
        </div>

        <!-- Sección: Grado y Curso -->
        <div class="form-section">
            <div class="form-section-title">
                <i class="bi bi-grid"></i>
                <span>Grado y Curso</span>
            </div>
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="GradoSeccionId" class="form-label-custom">
                        <i class="bi bi-mortarboard"></i> Grado/Sección
                    </label>
                    <select asp-for="GradoSeccionId" class="form-control form-control-custom" asp-items="ViewBag.GradoSecciones">
                        <option value="">Seleccione un grado/sección...</option>
                    </select>
                    <span asp-validation-for="GradoSeccionId" class="text-danger"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="Curso" class="form-label-custom">
                        <i class="bi bi-book"></i> Curso
                    </label>
                    <input asp-for="Curso" class="form-control form-control-custom" />
                    <span asp-validation-for="Curso" class="text-danger"></span>
                </div>
            </div>
        </div>

        <!-- Sección: Día y Horario -->
        <div class="form-section">
            <div class="form-section-title">
                <i class="bi bi-calendar-week"></i>
                <span>Día y Horario</span>
            </div>
            <div class="row">
                <div class="col-md-12 mb-3">
                    <label asp-for="DiaSemana" class="form-label-custom">
                        <i class="bi bi-calendar-day"></i> Día de la Semana
                    </label>
                    <select asp-for="DiaSemana" class="form-control form-control-custom">
                        <option value="">Seleccione un día...</option>
                        <option value="Lunes">Lunes</option>
                        <option value="Martes">Martes</option>
                        <option value="Miércoles">Miércoles</option>
                        <option value="Jueves">Jueves</option>
                        <option value="Viernes">Viernes</option>
                        <option value="Sábado">Sábado</option>
                    </select>
                    <span asp-validation-for="DiaSemana" class="text-danger"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="HoraInicio" class="form-label-custom">
                        <i class="bi bi-clock"></i> Hora de Inicio
                    </label>
                    <input asp-for="HoraInicio" type="time" class="form-control form-control-custom" />
                    <span asp-validation-for="HoraInicio" class="text-danger"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="HoraFin" class="form-label-custom">
                        <i class="bi bi-clock-fill"></i> Hora de Fin
                    </label>
                    <input asp-for="HoraFin" type="time" class="form-control form-control-custom" />
                    <span asp-validation-for="HoraFin" class="text-danger"></span>
                </div>
            </div>
        </div>

        <input type="hidden" asp-for="IsActive" />
    </div>

    <div class="modal-footer">
        <button type="button" class="btn btn-cancel" data-bs-dismiss="modal">
            <i class="bi bi-x-circle"></i> Cancelar
        </button>
        <button type="submit" class="btn btn-update">
            <i class="bi bi-check-circle"></i> Actualizar Horario
        </button>
    </div>
</form>

<script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
<script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>

<script>
    $(document).ready(function () {
        // Formatear horas para inputs tipo time
        var horaInicio = '@Model.HoraInicio.ToString(@"hh\:mm")';
        var horaFin = '@Model.HoraFin.ToString(@"hh\:mm")';
        $('input[name="HoraInicio"]').val(horaInicio);
        $('input[name="HoraFin"]').val(horaFin);

        $('#editForm').on('submit', function (e) {
            e.preventDefault();

            if (!$(this).valid()) {
                return false;
            }

            $.ajax({
                url: $(this).attr('action'),
                type: 'POST',
                data: $(this).serialize(),
                success: function () {
                    $('#editModal').modal('hide');
                    location.reload();
                },
                error: function (xhr) {
                    alert('Error al actualizar: ' + (xhr.responseText || 'Error desconocido'));
                }
            });
        });
    });
</script>