@model EduConectaPeru.Models.GradoSeccion

@{
    Layout = null;
}

<style>
    .modal-header-custom {
        background: linear-gradient(135deg, #FF6B6B 0%, #EE5A6F 100%);
        color: white;
        border-radius: 15px 15px 0 0;
        padding: 20px 25px;
    }

    .modal-body-custom {
        padding: 30px;
    }

    .alert-custom {
        background: linear-gradient(135deg, #fff3cd 0%, #ffeaa7 100%);
        border-left: 4px solid #f39c12;
        padding: 20px;
        border-radius: 10px;
        margin-bottom: 25px;
    }

        .alert-custom i {
            font-size: 2rem;
            color: #f39c12;
            margin-right: 15px;
        }

    .delete-info-box {
        background: #f8f9fa;
        border-radius: 10px;
        padding: 20px;
        margin-bottom: 20px;
    }

    .delete-info-row {
        display: flex;
        padding: 10px 0;
        border-bottom: 1px solid #dee2e6;
    }

        .delete-info-row:last-child {
            border-bottom: none;
        }

    .delete-info-label {
        font-weight: 600;
        color: #6c757d;
        min-width: 150px;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .delete-info-value {
        color: #212529;
        flex: 1;
    }

    .info-note {
        background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
        border-left: 4px solid #2196f3;
        padding: 15px;
        border-radius: 8px;
        margin-top: 20px;
    }

    .btn-cancel {
        background: #6c757d;
        color: white;
        border: none;
        padding: 12px 30px;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
    }

        .btn-cancel:hover {
            background: #5a6268;
            transform: translateY(-2px);
            color: white;
        }

    .btn-delete-confirm {
        background: linear-gradient(135deg, #FF6B6B 0%, #EE5A6F 100%);
        color: white;
        border: none;
        padding: 12px 30px;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(255, 107, 107, 0.4);
    }

        .btn-delete-confirm:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 107, 107, 0.6);
            color: white;
        }

    .grado-preview {
        text-align: center;
        padding: 20px;
        background: linear-gradient(135deg, #1976D2 0%, #1565C0 100%);
        border-radius: 10px;
        color: white;
        margin-bottom: 20px;
    }

        .grado-preview h4 {
            margin: 0;
            font-weight: 700;
        }

        .grado-preview p {
            margin: 5px 0 0 0;
            opacity: 0.9;
        }
</style>

<div class="modal-header modal-header-custom">
    <h5 class="modal-title">
        <i class="bi bi-exclamation-triangle"></i> Desactivar Grado/Sección
    </h5>
    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
</div>

<form id="deleteForm" asp-action="Delete" method="post">
    <div class="modal-body modal-body-custom">
        <input type="hidden" asp-for="GradoSeccionId" />

        <div class="alert-custom">
            <div class="d-flex align-items-center">
                <i class="bi bi-exclamation-triangle-fill"></i>
                <div>
                    <h6 class="mb-1" style="color: #856404; font-weight: 700;">¡Advertencia!</h6>
                    <p class="mb-0" style="color: #856404;">
                        Está a punto de desactivar este grado/sección. Los estudiantes ya matriculados mantendrán su registro.
                    </p>
                </div>
            </div>
        </div>

        <div class="grado-preview">
            <h4>@Model.Grado - Sección @Model.Seccion</h4>
            <p>Año Escolar @Model.AnioEscolar</p>
        </div>

        <div class="delete-info-box">
            <h6 class="mb-3" style="color: #495057; font-weight: 600;">
                <i class="bi bi-info-circle"></i> Información del Grado/Sección a Desactivar:
            </h6>
            <div class="delete-info-row">
                <div class="delete-info-label">
                    <i class="bi bi-hash"></i> ID:
                </div>
                <div class="delete-info-value">
                    <strong>#@Model.GradoSeccionId</strong>
                </div>
            </div>
            <div class="delete-info-row">
                <div class="delete-info-label">
                    <i class="bi bi-mortarboard"></i> Grado:
                </div>
                <div class="delete-info-value">
                    <strong>@Model.Grado</strong>
                </div>
            </div>
            <div class="delete-info-row">
                <div class="delete-info-label">
                    <i class="bi bi-grid-3x3-gap"></i> Sección:
                </div>
                <div class="delete-info-value">
                    <strong>@Model.Seccion</strong>
                </div>
            </div>
            <div class="delete-info-row">
                <div class="delete-info-label">
                    <i class="bi bi-calendar"></i> Año Escolar:
                </div>
                <div class="delete-info-value">
                    <strong>@Model.AnioEscolar</strong>
                </div>
            </div>
            <div class="delete-info-row">
                <div class="delete-info-label">
                    <i class="bi bi-people"></i> Capacidad:
                </div>
                <div class="delete-info-value">
                    <strong>@Model.Capacidad estudiantes</strong>
                </div>
            </div>
        </div>

        <div class="info-note">
            <i class="bi bi-info-circle"></i>
            <strong>Nota:</strong> El grado/sección puede ser reactivado posteriormente desde la lista principal usando el botón de reactivación. Los estudiantes ya matriculados no se verán afectados.
        </div>
    </div>

    <div class="modal-footer">
        <button type="button" class="btn btn-cancel" data-bs-dismiss="modal">
            <i class="bi bi-x-circle"></i> Cancelar
        </button>
        <button type="submit" class="btn btn-delete-confirm">
            <i class="bi bi-check-circle"></i> Confirmar Desactivación
        </button>
    </div>
</form>

<script>
    $(document).ready(function () {
        $('#deleteForm').on('submit', function (e) {
            e.preventDefault();

            $.ajax({
                url: $(this).attr('action'),
                type: 'POST',
                data: $(this).serialize(),
                success: function () {
                    $('#deleteModal').modal('hide');
                    location.reload();
                },
                error: function (xhr) {
                    alert('Error al desactivar: ' + (xhr.responseText || 'Error desconocido'));
                }
            });
        });
    });
</script>