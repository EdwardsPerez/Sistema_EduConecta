@model EduConectaPeru.Models.Student
@{
    Layout = null;
}

<style>
    .modal-header {
        background: linear-gradient(135deg, #FF6B6B 0%, #EE5A6F 100%);
        color: white;
        border: none;
        padding: 20px 30px;
    }

        .modal-header h5 {
            margin: 0;
            font-weight: 600;
            font-size: 1.3rem;
        }

        .modal-header .btn-close {
            filter: brightness(0) invert(1);
        }

    .modal-body {
        padding: 30px;
    }

    .alert-warning {
        background: linear-gradient(135deg, #FFF9C4 0%, #FFF59D 100%);
        border-left: 4px solid #FFC107;
        border-radius: 10px;
        padding: 15px;
        margin-bottom: 20px;
    }

        .alert-warning i {
            color: #F57C00;
            font-size: 1.5rem;
        }

        .alert-warning p {
            margin: 0;
            color: #F57C00;
            font-weight: 600;
        }

    .preview-box {
        background: #f8f9fa;
        border-radius: 15px;
        padding: 20px;
        margin-bottom: 20px;
        text-align: center;
    }

    .student-avatar-preview {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: linear-gradient(135deg, #FF6B6B 0%, #EE5A6F 100%);
        color: white;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 1.5rem;
        margin-bottom: 15px;
    }

    .preview-box h5 {
        margin: 10px 0 5px 0;
        color: #2c3e50;
        font-weight: 700;
    }

    .preview-box p {
        margin: 0;
        color: #6c757d;
    }

    .info-box {
        background: #f8f9fa;
        border-left: 4px solid #6c757d;
        padding: 15px;
        border-radius: 10px;
        margin-bottom: 20px;
    }

    .info-row {
        display: flex;
        justify-content: space-between;
        padding: 8px 0;
        border-bottom: 1px solid #e9ecef;
    }

        .info-row:last-child {
            border-bottom: none;
        }

    .info-label {
        font-weight: 600;
        color: #495057;
    }

    .info-value {
        color: #212529;
    }

    .btn-confirm-delete {
        background: linear-gradient(135deg, #FF6B6B 0%, #EE5A6F 100%);
        color: white;
        border: none;
        padding: 12px 30px;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s ease;
    }

        .btn-confirm-delete:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 107, 107, 0.4);
            color: white;
        }
</style>

<div class="modal-header">
    <h5><i class="bi bi-trash-fill"></i> Confirmar Desactivación</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>

<form id="deleteForm" asp-action="Delete" method="post">
    <input type="hidden" asp-for="StudentId" />

    <div class="modal-body">
        <div class="alert-warning">
            <div class="d-flex align-items-center gap-3">
                <i class="bi bi-exclamation-triangle-fill"></i>
                <div>
                    <p class="mb-1"><strong>¡Advertencia!</strong></p>
                    <p class="mb-0" style="font-weight: 400;">
                        Está a punto de desactivar este estudiante. Esta acción puede ser revertida posteriormente.
                    </p>
                </div>
            </div>
        </div>

        <div class="preview-box">
            <div class="student-avatar-preview">
                @Model.Nombre.Substring(0, 1)@Model.Apellido.Substring(0, 1)
            </div>
            <h5>@Model.Nombre @Model.Apellido</h5>
            <p>DNI: @Model.DNI</p>
        </div>

        <div class="info-box">
            <div class="info-row">
                <div class="info-label">
                    <i class="bi bi-hash"></i> ID Estudiante:
                </div>
                <div class="info-value">
                    <strong>#@Model.StudentId</strong>
                </div>
            </div>

            <div class="info-row">
                <div class="info-label">
                    <i class="bi bi-person"></i> Nombre Completo:
                </div>
                <div class="info-value">
                    @Model.Nombre @Model.Apellido
                </div>
            </div>

            <div class="info-row">
                <div class="info-label">
                    <i class="bi bi-card-text"></i> DNI:
                </div>
                <div class="info-value">
                    @Model.DNI
                </div>
            </div>

            <div class="info-row">
                <div class="info-label">
                    <i class="bi bi-calendar"></i> Fecha de Nacimiento:
                </div>
                <div class="info-value">
                    @Model.FechaNacimiento.ToString("dd/MM/yyyy")
                </div>
            </div>

            @if (Model.LegalGuardian != null)
            {
                <div class="info-row">
                    <div class="info-label">
                        <i class="bi bi-person-badge"></i> Apoderado:
                    </div>
                    <div class="info-value">
                        @Model.LegalGuardian.Nombre @Model.LegalGuardian.Apellido
                    </div>
                </div>
            }
        </div>

        <div style="background: #FFF3CD; border-left: 4px solid #FFC107; padding: 15px; border-radius: 10px;">
            <p class="mb-0" style="color: #856404;">
                <i class="bi bi-info-circle-fill"></i>
                <strong>Nota:</strong> El estudiante quedará inactivo y no aparecerá en los listados principales. Puede reactivarlo en cualquier momento.
            </p>
        </div>
    </div>

    <div class="modal-footer" style="border-top: 1px solid #dee2e6; padding: 20px 30px;">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
            <i class="bi bi-x-circle"></i> Cancelar
        </button>
        <button type="submit" class="btn-confirm-delete">
            <i class="bi bi-trash"></i> Confirmar Desactivación
        </button>
    </div>
</form>

<script>
    $(document).ready(function() {
        $('#deleteForm').on('submit', function(e) {
            e.preventDefault();

            var formData = $(this).serialize();

            $.ajax({
                url: $(this).attr('action'),
                type: 'POST',
                data: formData,
                success: function(response) {
                    $('#deleteModal').modal('hide');
                    location.reload();
                },
                error: function() {
                    alert('Error al desactivar el estudiante. Por favor, intente nuevamente.');
                }
            });
        });
    });
</script>